# 개발 기록

## 2026-08-16 — CIELAB/OKLab 기반 PPT 색조 도구

**요청 배경**: CIELAB을 이용해 PPT에 원하는 색조를 설정하고 싶다는 질문에서 시작해서, 실제 랩미팅 타이틀 슬라이드(`YCL_lab_meeting08.03`)에 그라데이션을 적용하는 작업으로 확장됨.

### 진행 순서

1. **CIELAB 톤 팔레트 도구 최초 구현** (`c6b0a66`, `d13ca61`)
   - `cielab_ppt/color_lab.py`: 외부 의존성 없는 순수 Python sRGB↔CIELAB 변환.
   - `cli.py`에 `palette`/`theme`/`swatches` 서브커맨드. 색조(hue) 고정, 명도(L\*)만 계단식으로 바꾸는 팔레트 생성.

2. **CIELAB → OKLab 전환** (`8736258`)
   - CIELAB은 파란색 계열에서 명도가 바뀔 때 색조가 보라 쪽으로 틀어지는 문제가 있어 OKLab으로 교체 요청받음.
   - `cielab_ppt/` → `oklab_ppt/`로 개명, `color_oklab.py`에 Björn Ottosson의 OKLab 변환식 구현.

3. **실제 파일에 그라데이션 적용** (`4bd39ab`)
   - 사용자가 업로드한 `YCL_lab_meeting08.03.pptx`의 배경 사각형(`직사각형 2`)에 그라데이션을 입히고 싶다는 요청.
   - `gradient` 서브커맨드 신설: 기존 pptx의 지정한 도형을 찾아 `a:gradFill` XML을 직접 조작(python-pptx가 테마 색 편집을 공식 지원하지 않아 lxml로 직접 편집).
   - 첫 시도가 너무 어둡다는 피드백 → `--l-min`을 0.10 → 0.20으로 조정.

4. **두 색 보간(빨강→노랑) 지원** (`5e2a31d`)
   - "색조 유지"가 아니라 "색 자체를 바꾸는" 그라데이션 요청.
   - `interpolate_tone_palette()` 추가: OKLab 공간에서 직선 보간(RGB 직접 보간 시 생기는 탁한 중간색 문제 회피). `--color2` 옵션으로 모든 서브커맨드에 적용.
   - 밝은 노랑은 흰 텍스트와 대비가 나빠서, 대비 계산 후 진한 골드 톤으로 조정.

5. **인터랙티브 브라우저 도구 제작** ("OKLab Gradient Studio" 아티팩트)
   - 매번 CLI로 왕복하지 않고 눈으로 직접 색을 조절하고 싶다는 요청.
   - 색상 피커/hex 입력, 명도 범위·단계 수·각도 슬라이더, 16:9 실시간 미리보기(실제 슬라이드 문구 그대로 사용), 텍스트 대비 배지, hex 스와치, 재현용 CLI 명령 자동 생성까지 포함한 단일 HTML 아티팩트.
   - Playwright로 라이트/다크/모바일 레이아웃, JS 콘솔 에러 없음을 매 변경마다 검증.

6. **OKLCH 직접 조절 추가**
   - hex만으로는 부족하다는 요청 → 색상별로 L(명도)/C(채도)/H(색상각) 슬라이더 추가, hex 입력과 완전히 양방향 동기화.
   - 슬라이더 트랙 배경을 실제 색 스펙트럼(gamut-mapped)으로 실시간 렌더링해서 "지금 어디를 눌러야 어떤 색이 나오는지" 감이 오도록 함.

7. **텍스트 색 제어 추가** (`7697af5`)
   - 스튜디오에서 검정으로 지정한 텍스트 색을 CLI 쪽에서는 알 수 없다는 문제 발견.
   - `--text-color`/`--text-shapes` 옵션 추가: 지정한 도형(제목/부제목)의 텍스트 런 색을 실제로 덮어씀. 스튜디오의 CLI 명령 생성기에도 이 값을 포함시켜 "화면에서 조절한 값 = 나에게 전달되는 값"이 되도록 루프를 닫음.

8. **그라데이션 유형·위치 확장** (`e190036`, `443fc1a`)
   - "한 방향만 말고 여러 방향/위치도" 요청.
   - `--type linear|radial|rectangular`, `--center`(키워드 또는 `X%,Y%`) 추가. OOXML `a:path` 그라데이션으로 구현.
   - 이어서 "여러 위치에서 동시에 시작"까지 요청 → OOXML gradFill이 중심을 하나만 지원하는 한계를 도형 복제로 우회: 위치마다 도형을 복제하고 바깥쪽 색을 투명하게 페이드시켜 겹쳐 쌓음(텍스트보다 뒤, 원본보다 앞에 z-order 정확히 삽입). `--center`를 `;`로 구분해 여러 개 지정.
   - 스튜디오의 중심 위치 그리드도 다중 선택으로 변경, CSS `radial-gradient` 레이어를 겹쳐서 근사 미리보기.

### 결과물

- `oklab_ppt/color_oklab.py`, `oklab_ppt/cli.py`, `oklab_ppt/requirements.txt` — 재사용 가능한 CLI 도구.
- `README.md`에 사용법 7가지 예시 전부 반영.
- **OKLab Gradient Studio** 아티팩트: https://claude.ai/code/artifact/5ff9eb34-b19c-4a97-a35c-611ef2c1a3ac
- 실제 `YCL_lab_meeting08.03.pptx` 파생본 여러 개(단색조 그라데이션, 빨강→골드, 다중 중심 방사형 등) 생성해서 전달.

### 알아둘 점 / 한계

- 이 샌드박스에서는 LibreOffice headless 변환이 동작하지 않아, pptx 결과물을 스크린샷으로 직접 확인하지 못했음(XML 구조 검증 + python-pptx 재로딩으로만 확인). 실제 PowerPoint에서 최종 확인 필요.
- 스튜디오의 "사각형(rectangular)" 그라데이션 미리보기는 CSS에 대응 기능이 없어 타원형(radial)으로 근사함 — 실제 pptx의 사각형 경로 그라데이션과는 약간 다르게 보일 수 있음.
- 다중 중심 그라데이션은 도형을 겹쳐 쌓는 우회 구현이라, 같은 `--shape` 이름을 가진 도형이 이미 여러 장 있는 파일에 반복 실행하면 레이어가 계속 늘어날 수 있음(항상 원본 템플릿에서 새로 생성하는 워크플로 기준으로 설계됨).
